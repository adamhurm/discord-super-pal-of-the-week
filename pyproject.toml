[tool.ruff]
line-length = 100
src = ["src"]

[tool.ruff.lint]
select = ["E", "W", "F", "I", "UP", "B", "C4", "RUF"]

[tool.ruff.lint.isort]
known-first-party = ["superpal"]

[tool.ty.environment]
python-version = "3.13"
